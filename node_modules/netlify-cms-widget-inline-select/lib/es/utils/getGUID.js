import { NAME } from '../constants';
var counter = 0;
var prefix;
export default (function () {
  counter += 1;
  prefix = prefix || new Date().getTime() + "_" + Math.random();
  return prefix + "__" + NAME + "__" + counter;
});
//# sourceMappingURL=getGUID.js.map